@page
@model BITS_Project.Pages.Tournaments.EditModel

@{
    ViewData["Title"] = "Edit";
    if (Model.SignedIn == 1)
    {
        Layout = "_EmployeeLayout";
    }}

<h1>Edit</h1>

<h4>Tournament</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Tournament.ID" />
            <div class="form-group">
                <label asp-for="Tournament.DateMade" class="control-label"></label>
                <input asp-for="Tournament.DateMade" class="form-control" />
                <span asp-validation-for="Tournament.DateMade" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tournament.DateFor" class="control-label"></label>
                <input asp-for="Tournament.DateFor" class="form-control" />
                <span asp-validation-for="Tournament.DateFor" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tournament.ActivityType" class="control-label"></label>
                <select asp-for="Tournament.ActivityType" class="form-control"></select>
                <span asp-validation-for="Tournament.ActivityType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
